import useStyles from '#/assets/css/components/tab-separate.style'

const TabSeparate = () => {
    const { styles } = useStyles()

    return <div className={styles.root} />
}

export default TabSeparate
